import LogisticsLayout from '@/components/logistics/LogisticsLayout';

export default function LogisticaModuleLayout({ children }: { children: React.ReactNode }) {
  return <LogisticsLayout>{children}</LogisticsLayout>;
}
